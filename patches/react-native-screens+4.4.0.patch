diff --git a/node_modules/react-native-screens/ios/RNSScreen.mm b/node_modules/react-native-screens/ios/RNSScreen.mm
index 6d05957..3a3820c 100644
--- a/node_modules/react-native-screens/ios/RNSScreen.mm
+++ b/node_modules/react-native-screens/ios/RNSScreen.mm
@@ -2076,3 +2076,22 @@ RCT_ENUM_CONVERTER(
 // So that the define-macro is not leaked out of this file.
 // This one is defined in very top of the file depending on RN architecture.
 #undef ReactScrollViewBase
+
+#ifdef RCT_NEW_ARCH_ENABLED
+#if RCT_NEW_ARCH_ENABLED
+- (RCTSurfaceTouchHandler *)rnscreens_findTouchHandlerInAncestorChain
+#else
+- (RCTTouchHandler *)rnscreens_findTouchHandlerInAncestorChain
+#endif
+{
+#if RCT_NEW_ARCH_ENABLED
+  return (RCTSurfaceTouchHandler *)_touchHandler;
+#else
+  return (RCTTouchHandler *)_touchHandler;
+#endif
+}
+
+- (void)didUpdateReactSubviews
+{
+  // ... existing code ...
+}
